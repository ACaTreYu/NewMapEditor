# Milestone v3.7: Sidebar Independence

**Status:** ✅ SHIPPED 2026-02-16
**Phases:** 80
**Total Plans:** 1

## Overview

Decouple minimap from animations panel — minimap stays fixed in corner, animations panel collapses independently via frame border toggle, canvas expands to fill freed space.

## Phases

### Phase 80: Sidebar Independence ✓

**Goal**: User can collapse animations panel to maximize canvas while minimap stays visible in fixed corner

**Depends on**: Phase 79

**Requirements**: SIDE-01, SIDE-02, SIDE-03, SIDE-04, SIDE-05

**Success Criteria** (what must be TRUE):
  1. User can click frame border toggle to collapse animations panel
  2. When animations panel is collapsed, minimap remains visible in top-right corner
  3. When animations panel is collapsed, editing canvas expands horizontally to fill freed space
  4. When animations panel is expanded, current stacked layout preserved with minimap on top
  5. Game object tool panel collapses/expands with animations panel

Plans:
- [x] 80-01-PLAN.md — Minimap overlay + collapsible sidebar + toolbar toggle

---

## Milestone Summary

**Key Decisions:**
- Minimap stays in sidebar column (not absolute overlay) — preserves stacked layout feel
- Frame border collapse toggle (thin vertical strip with chevron) instead of toolbar button — matches existing UX pattern
- Sidebar always visible for minimap, only animations panel content conditionally rendered

**Issues Resolved:**
- None

**Issues Deferred:**
- None

**Technical Debt Incurred:**
- None

---

_For current project status, see .planning/ROADMAP.md_
