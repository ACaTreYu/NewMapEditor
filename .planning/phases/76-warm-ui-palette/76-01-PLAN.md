---
phase: 76-warm-ui-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/styles/variables.css]
autonomous: false

must_haves:
  truths:
    - "Neutral palette primitives (50-700) use warm hue (~50 degrees) instead of cool hue (280 degrees)"
    - "All UI surfaces, backgrounds, borders, and hover states appear warm-toned (cream/beige direction)"
    - "Contrast and readability maintained — text remains legible on all backgrounds"
    - "Accent blue colors unchanged — buttons, focus rings, and highlights remain blue"
    - "Achromatic extremes preserved — neutral-0 stays pure white, neutral-900 stays near-black"
  artifacts:
    - path: "src/styles/variables.css"
      provides: "Warm neutral OKLCH palette primitives"
      contains: "oklch.*0\\.005 50"
  key_links:
    - from: "src/styles/variables.css (Tier 1 primitives)"
      to: "src/styles/variables.css (Tier 2 semantic aliases)"
      via: "var(--color-neutral-*) references"
      pattern: "var\\(--color-neutral-"
---

<objective>
Shift the UI color palette from cool blue-grey (hue 280) to warm cream/beige (hue 50) by updating 5 OKLCH neutral primitive hue values in variables.css.

Purpose: Create a warmer, more inviting interface feel while maintaining all contrast ratios and readability. The two-tier design token system cascades the change through 327 semantic token usages across 18 component CSS files automatically.

Output: Updated `src/styles/variables.css` with warm neutral palette.
</objective>

<execution_context>
@C:\Users\arcje\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\arcje\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-warm-ui-palette/76-RESEARCH.md
@src/styles/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update neutral palette hue values from 280 to 50</name>
  <files>src/styles/variables.css</files>
  <action>
In `src/styles/variables.css`, update the OKLCH hue value from `280` to `50` for the following 5 neutral palette primitives:

1. `--color-neutral-50`: `oklch(98% 0.005 280)` -> `oklch(98% 0.005 50)`
2. `--color-neutral-100`: `oklch(95% 0.005 280)` -> `oklch(95% 0.005 50)`
3. `--color-neutral-200`: `oklch(90% 0.005 280)` -> `oklch(90% 0.005 50)`
4. `--color-neutral-300`: `oklch(80% 0.005 280)` -> `oklch(80% 0.005 50)`
5. `--color-neutral-500`: `oklch(60% 0.005 280)` -> `oklch(60% 0.005 50)`

Also update the `--color-neutral-700` line. It currently has hue 280:
6. `--color-neutral-700`: `oklch(35% 0.005 280)` -> `oklch(35% 0.005 50)`

DO NOT change:
- `--color-neutral-0`: Keep as `oklch(100% 0 0)` (pure white, achromatic)
- `--color-neutral-900`: Keep as `oklch(20% 0 0)` (near-black, achromatic)
- Any `--color-blue-*` values (accent colors stay at hue 250)
- Any lightness (L) or chroma (C) values — only change hue (H)

Update the comment on the neutral palette section from "Cool tone (slight blue-grey)" to "Warm tone (cream/beige direction)".

This is a simple find-and-replace of `280` -> `50` within the 6 neutral color lines (neutral-50 through neutral-700). No other values in the file use hue 280, so this is safe.
  </action>
  <verify>
Read the updated file and confirm:
- 6 neutral primitives (50, 100, 200, 300, 500, 700) all have hue value `50`
- neutral-0 remains `oklch(100% 0 0)`
- neutral-900 remains `oklch(20% 0 0)`
- blue-400, blue-500, blue-600 remain at hue `250`
- All lightness and chroma values unchanged
- Run `npm run typecheck` to confirm no build errors
  </verify>
  <done>All 6 neutral palette hue values changed from 280 to 50. Achromatic extremes and accent blues unchanged. No build errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of warm palette</name>
  <what-built>Warm neutral UI palette — all surfaces, backgrounds, borders, hover states shifted from cool blue-grey to warm cream/beige by changing OKLCH hue from 280 to 50 in CSS design tokens.</what-built>
  <how-to-verify>
1. Run `npm run electron:dev` to launch the editor
2. Overall feel: UI should feel noticeably warmer — cream/beige undertones instead of cool blue-grey
3. Toolbar: Hover over toolbar buttons — hover state should show warm highlight, clearly distinguishable from rest state
4. Panels: Open Map Settings panel (gear icon) — backgrounds should appear warm white, not pure white
5. Borders: Panel borders and dividers should have warm grey tone, not cool grey
6. Input focus: Click an input field — blue focus ring should stand out clearly against warm background
7. Accent colors: Blue buttons and highlights should remain blue (not shifted warm)
8. Text readability: All text should remain clearly legible — no contrast issues
9. Scrollbars: Track and thumb should match warm palette
10. If warmth feels too subtle or too strong, feedback on hue adjustment (+/- 10 from 50)
  </how-to-verify>
  <resume-signal>Type "approved" if warm palette looks good, or describe adjustments needed (e.g., "too yellow, try hue 60" or "too subtle, try hue 40")</resume-signal>
</task>

</tasks>

<verification>
- `src/styles/variables.css` contains 6 neutral primitives with hue `50` (was `280`)
- Neutral-0 and neutral-900 remain achromatic (C=0, H=0)
- Accent blues remain at hue 250
- `npm run typecheck` passes
- Visual inspection confirms warm tone across all UI surfaces
</verification>

<success_criteria>
- OKLCH neutral palette uses warm hue 50 instead of cool hue 280
- All UI surfaces, backgrounds, borders, and hover states reflect warm palette
- Text remains legible with maintained contrast ratios
- Accent blue colors unchanged
- User visually approves the warm palette appearance
</success_criteria>

<output>
After completion, create `.planning/phases/76-warm-ui-palette/76-01-SUMMARY.md`
</output>
