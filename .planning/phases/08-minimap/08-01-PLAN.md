---
phase: 08-minimap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Minimap/Minimap.css
autonomous: true

must_haves:
  truths:
    - "Minimap displays in top-right corner of the interface"
    - "Minimap shows entire 256x256 map scaled to fit (128x128 canvas)"
    - "Minimap renders viewport indicator showing currently visible map area"
    - "Clicking minimap navigates canvas to that location"
  artifacts:
    - path: "src/components/Minimap/Minimap.css"
      provides: "Top-right positioning"
      contains: "top:"
  key_links:
    - from: "src/components/Minimap/Minimap.css"
      to: "src/components/Minimap/Minimap.tsx"
      via: "CSS class import"
      pattern: "\\.minimap"
---

<objective>
Reposition existing minimap from bottom-left to top-right corner

Purpose: Research revealed the Minimap component already implements ALL functional requirements (256x256 rendering, viewport indicator, click-to-navigate). The only missing piece is positioning - it's currently bottom-left but requirements specify top-right.

Output: Minimap appears in top-right corner, all existing functionality preserved
</objective>

<execution_context>
@C:\Users\arcje\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\arcje\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-minimap/08-RESEARCH.md
@src/components/Minimap/Minimap.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition minimap to top-right</name>
  <files>src/components/Minimap/Minimap.css</files>
  <action>
Update the .minimap CSS class positioning:

Current (bottom-left):
```css
.minimap {
  position: absolute;
  bottom: 20px;
  left: 8px;
  ...
}
```

Change to (top-right):
```css
.minimap {
  position: absolute;
  top: 8px;
  right: 8px;
  ...
}
```

This is a 2-property change:
- Remove `bottom: 20px;` and replace with `top: 8px;`
- Remove `left: 8px;` and replace with `right: 8px;`

Keep all other properties unchanged (z-index, background, border, padding, box-shadow).
  </action>
  <verify>
Run `npm run electron:dev`, verify minimap appears in top-right corner of the map canvas area (not the window).

Visual checks:
1. Minimap displays in top-right corner
2. Minimap shows entire map scaled down (128x128 pixels)
3. White viewport rectangle shows current visible area
4. Click anywhere on minimap - main canvas navigates to that location
5. Drag on minimap - viewport follows smoothly
  </verify>
  <done>
Minimap positioned in top-right corner with 8px margins from top and right edges. All existing functionality (map rendering, viewport indicator, click-to-navigate, drag support) works unchanged.
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Launch app: `npm run electron:dev`
2. Confirm minimap visible in top-right corner of canvas area
3. Load a map with varied tiles - confirm minimap shows color representation
4. Zoom main canvas - viewport rectangle in minimap should resize
5. Pan main canvas - viewport rectangle should follow
6. Click minimap corners - canvas should navigate to map corners
7. Drag across minimap - canvas should pan continuously

All requirements from ROADMAP:
- [x] MINI-01: Minimap displays in top-right corner
- [x] MINI-02: Minimap shows entire 256x256 map scaled down
- [x] MINI-03: Minimap shows viewport indicator
- [x] MINI-04: Clicking minimap navigates to that location
</verification>

<success_criteria>
- Minimap visible in top-right corner (not bottom-left)
- 8px margin from top and right edges
- All existing minimap functionality unchanged
- No z-index conflicts with other UI elements
</success_criteria>

<output>
After completion, create `.planning/phases/08-minimap/08-01-SUMMARY.md`
</output>
