---
phase: 75-tool-icon-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ToolBar/ToolBar.tsx]
autonomous: true

must_haves:
  truths:
    - "Bunker tool button shows a castle/fortress icon (not a generic shield)"
    - "Conveyor tool button shows a conveyor belt icon (not a generic arrow)"
    - "All tool icons in the toolbar are visually distinct from each other"
  artifacts:
    - path: "src/components/ToolBar/ToolBar.tsx"
      provides: "Updated icon imports and toolIcons record"
      contains: "LuCastle"
  key_links:
    - from: "toolIcons record"
      to: "renderToolButton function"
      via: "toolIcons[tool.icon] lookup"
      pattern: "bunker: LuCastle"
---

<objective>
Replace generic placeholder icons for bunker (LuShield) and conveyor (LuArrowRight) tools with semantically appropriate Lucide icons: LuCastle for bunker and LuBriefcaseConveyorBelt for conveyor.

Purpose: Make game object tool icons visually distinct and immediately recognizable, matching the pattern established in Phase 71 for wall tools.
Output: Updated ToolBar.tsx with new icon imports and toolIcons record entries.
</objective>

<execution_context>
@C:\Users\arcje\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\arcje\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ToolBar/ToolBar.tsx (lines 1-48 — imports and toolIcons record)
@.planning/phases/75-tool-icon-polish/75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bunker and conveyor icon imports and toolIcons entries</name>
  <files>src/components/ToolBar/ToolBar.tsx</files>
  <action>
In ToolBar.tsx, make two changes:

1. **Update import statement** (line 19): Replace `LuShield` with `LuCastle` and `LuArrowRight` with `LuBriefcaseConveyorBelt` in the react-icons/lu import block. The line currently reads:
   ```
   LuShield, LuBox, LuArrowRightLeft, LuArrowRight,
   ```
   Change it to:
   ```
   LuCastle, LuBox, LuArrowRightLeft, LuBriefcaseConveyorBelt,
   ```

2. **Update toolIcons record** (lines 43 and 46):
   - Line 43: Change `bunker: LuShield,` to `bunker: LuCastle,`
   - Line 46: Change `conveyor: LuArrowRight,` to `conveyor: LuBriefcaseConveyorBelt,`

No other changes needed. The renderToolButton function consumes toolIcons generically — icon changes propagate automatically.

Do NOT change the toolIcons keys ('bunker', 'conveyor') — only change the icon component values.
  </action>
  <verify>
Run `npm run typecheck` — should pass with no new errors (pre-existing errors in MapCanvas/CanvasEngine are expected and unrelated).

Grep for `LuShield` and `LuArrowRight` in ToolBar.tsx — neither should appear (both fully replaced). Grep for `LuCastle` and `LuBriefcaseConveyorBelt` — both should appear in imports and in toolIcons record.
  </verify>
  <done>
Bunker tool uses LuCastle icon. Conveyor tool uses LuBriefcaseConveyorBelt icon. No LuShield or LuArrowRight references remain in ToolBar.tsx. TypeScript compiles without new errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes (no new errors)
2. `LuShield` does not appear in ToolBar.tsx
3. `LuArrowRight` does not appear in ToolBar.tsx
4. `LuCastle` appears in both import and toolIcons record
5. `LuBriefcaseConveyorBelt` appears in both import and toolIcons record
6. toolIcons keys unchanged: `bunker` and `conveyor` still present
</verification>

<success_criteria>
- Bunker tool button renders a castle icon (visually distinct fortress/defensive structure)
- Conveyor tool button renders a conveyor belt icon (visually distinct transport mechanism)
- All tool icons in toolbar are visually unique — no two tools share the same icon appearance
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/75-tool-icon-polish/75-01-SUMMARY.md`
</output>
